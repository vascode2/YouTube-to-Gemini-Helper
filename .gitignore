# Runtime diagnostic logs (generated by copy.ahk / Hammerspoon)
copyurl_log.txt
copyurl_log.txt.1
copyurl_last_winner.txt

# Generated test log
scripts/chrome_test_log.txt

# Local VS Code workspace config (machine-specific)
*.code-workspace

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/

# Dependencies (recreate with `npm install`)
node_modules/
# NOTE: package-lock.json is intentionally tracked for reproducible installs.

# Safety: never commit secrets, but keep example templates
.env
.env.*
!.env.example
